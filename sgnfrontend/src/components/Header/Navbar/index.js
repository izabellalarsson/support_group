import React from 'react';
import PropTypes from 'prop-types';
import styled from ''



const Navbar = props => {
    return (
        <div>
            
        </div>
    );
};

Navbar.propTypes = {
    
};

export default Navbar;